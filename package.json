{
  "name": "hooks-todos",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "format": "prettier --single-quote --arrow-parens always --prose-wrap never --print-width 150 --write src/**/*.js"
  },
  "dependencies": {
    "axios":"^0.18.0",
    "react":"^16.8.3",
    "react-dom":"^16.8.3",
    "react-scripts":"^2.1.5",
    "uuid":"^3.3.2"
  },  
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
